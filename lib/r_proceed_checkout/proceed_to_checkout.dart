import 'package:flutter/material.dart';

class ProceedToCheckoutPage extends StatelessWidget {
  const ProceedToCheckoutPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Checkout Page'),
      ),
    );
  }
}
